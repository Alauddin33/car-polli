import React from 'react';
import Spinner from '../../../Spinner/Spinner';
import Carousel from '../Carousel/Carousel';

const Home = () => {
    return (
        <div>
            <Carousel></Carousel>
            <Spinner></Spinner>
        </div>
    );
};

export default Home;